<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pmp.hrsrc.mapper.UserMapper">
    <select id="selectAll" resultType="com.pmp.hrsrc.entity.User">
        select * from t_user ;
    </select>
    <select id="selectById" parameterType="int" resultType="com.pmp.hrsrc.entity.User">
        select * from t_user where id =#{id}
    </select>
    <select id="selectMaxId" resultType="int">
        select ifnull(max(id),0)+1 from t_user ;
    </select>
    <select id="validateUser" parameterType="com.pmp.hrsrc.entity.User" resultType="com.pmp.hrsrc.entity.User">
        select * from t_user where uname=#{uname}
    </select>

    <insert id="insertUser" parameterType="com.pmp.hrsrc.entity.User" >
        insert into t_user(id,uname,upass,val,lodate)
        values(#{id},#{uname},#{upass},#{val},#{lodate})
    </insert>
    <update id ="updateUser" parameterType="com.pmp.hrsrc.entity.User">
        update t_user set upass=#{upass},uname=#{uname} where id=#{id}
    </update>

    <update id ="updateDateAndIp" parameterType="com.pmp.hrsrc.entity.User">
        update t_user set lodate =#{lodate},ip =#{ip} where id =#{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from t_user where id=#{id}
        delete from t_sec  where uid =#{id}
    </delete>
</mapper>